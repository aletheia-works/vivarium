# Reproduction — {{TITLE}}

> Layer 2 (Docker) reproduction. Conforms to the catalogue
> conventions in [`src/layer2_docker/README.md`](../README.md);
> see [`.claude/rules/recipe-authoring.md`](../../../.claude/rules/recipe-authoring.md)
> for the agent-side authoring checklist (path-scoped, auto-loads
> when Claude Code edits files under `src/layer*_*/`).

## The behaviour

TODO: describe the bug in 1–3 paragraphs. Include:

- The minimal reproducer (the same logic the recipe's `repro.sh`
  exercises).
- Expected vs. observed behaviour, with a concrete example.
- The affected version range and any known regression boundaries.

Upstream: [{{PROJECT}}#{{ISSUE}}]({{ISSUE_URL}})

## Why this bug

TODO: 2–4 bullets on why the bug is a good fit for Layer 2 and
why the recipe is interesting (catalogue-fit, fix-detection
sentinel value, real-toolchain semantics, etc.).

## Files

| File           | Role                                                    |
| -------------- | ------------------------------------------------------- |
| `Dockerfile`   | Pinned base image; copies `repro.sh`; sets CMD.         |
| `repro.sh`     | Runs the probe, emits verdict JSON, exits 0 / 1.        |
| `README.md`    | This file.                                              |
| `verdict.json` | *Generated by CI.* Captures verdict + image digest + run timestamp. Surfaced on the gallery. Gitignored locally. |

## Verdict contract

- `reproduced` ⇔ TODO: define the boolean condition `repro.sh` checks.
- `unreproduced` ⇔ TODO: negation of the above (state explicitly).

## Running locally — `docker run`

```bash
docker run --rm ghcr.io/aletheia-works/vivarium-{{SLUG}}:latest
```

## Building locally — `docker build`

```bash
cd src/layer2_docker/{{SLUG}}
docker build -t vivarium-{{SLUG}}:dev .
docker run --rm vivarium-{{SLUG}}:dev
```

## References

- Upstream issue: [{{PROJECT}}#{{ISSUE}}]({{ISSUE_URL}})
- TODO: link any spec / standards / discussion threads relevant to
  understanding the bug.

## Deployment

Image is built and pushed to GHCR by `deploy-docs.yml` on push to `main`:

```text
ghcr.io/aletheia-works/vivarium-{{SLUG}}:latest
ghcr.io/aletheia-works/vivarium-{{SLUG}}:<git-sha>
```

`verdict.json` lands in the GitHub Pages artefact at
`/repro/{{PROJECT}}/{{ISSUE}}/verdict.json`.
